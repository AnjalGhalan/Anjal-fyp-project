 Weekly Log (This Week)
What I did:

Fixed errors with running the Django server and successfully launched the project.
Learned how to access different pages: Home, Login, Register, Dashboard, and Admin.
Registered models (Student, Merit, Demerit) in the admin panel for data management.

 Next Week Plan
To Do:

Add separate pages to display Merit and Demerit records.
Create a student profile page to show individual behavior reports.
Start preparing simple slides for mid-progress presentation.




 Proof of Work (Code Snippets)

 admin.py

from django.contrib import admin
from .models import Student, Merit, Demerit

admin.site.register(Student)
admin.site.register(Merit)
admin.site.register(Demerit)


views.py (added registration + dashboard)

from django.shortcuts import render, redirect
from django.contrib.auth import authenticate, login, logout
from django.contrib.auth.models import User
from django.contrib import messages

def home(request):
    return render(request, 'home.html')

def user_login(request):
    if request.method == 'POST':
        username = request.POST['username']
        password = request.POST['password']
        user = authenticate(request, username=username, password=password)
        if user is not None:
            login(request, user)
            return redirect('dashboard')
    return render(request, 'login.html')

def register(request):
    if request.method == 'POST':
        username = request.POST['username']
        password = request.POST['password']
        confirm_password = request.POST['confirm_password']

        if password == confirm_password:
            if User.objects.filter(username=username).exists():
                messages.error(request, "Username already taken.")
            else:
                User.objects.create_user(username=username, password=password)
                messages.success(request, "Registration successful! You can now log in.")
                return redirect('login')
        else:
            messages.error(request, "Passwords do not match.")
    return render(request, 'register.html')

def dashboard(request):
    return render(request, 'dashboard.html')
